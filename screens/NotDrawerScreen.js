import { View, Text, Button } from 'react-native';
import * as React from 'react';

export default function NotDrawerScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Not Drawer Screen</Text>
      </View>
    );
  }